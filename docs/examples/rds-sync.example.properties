# =============================================================================
# rds-sync → MySQL 目标示例（架构配置键；P1 起由 Client 读取）
# =============================================================================
source.uri=jdbc:mysql://127.0.0.1:3306/demo?useSSL=false
target.type=mysql
target.uri=jdbc:mysql://127.0.0.1:3306/demo_dst?useSSL=false
sync.mode=FULL
bootstrap.table=true
